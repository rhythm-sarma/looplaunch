# Execution Layer (Layer 3)

This directory contains deterministic Python scripts that handle API calls, data processing, file operations, and external services.

- Scripts should be modular, testable, and well-commented.
- Credentials and environment variables are loaded from `.env`.
- All intermediate data processing should store temporary files in `.tmp/`.
